# ポケスリ育成コンパス Ver.1.0.3

ポケモンスリープの育成・厳選・島選択を整理するためのPWAです。

アプリが最適解を決めるのではなく、ユーザー自身が判断できるように、登録個体、図鑑設定、種族候補、目標、Progress、ToDo、ホーム、島、役割画面、未設定一覧をつなげて表示します。

## Ver1.0.3の修正内容

- 個体登録 / 編集と図鑑検索で、入力中に候補が勝手に確定されないように修正
- 個体登録時の種族初期値を空欄に変更
- 個体登録 / 編集の食材選択を、対象ポケモンの食材候補内に制限
- 個体詳細の役割編集で、未設定の項目が自動選択されないように修正
- 島の現在戦力を、その島の好物タイプに対応する「きのみ担当」「エナジー担当」だけに修正
- 島の現在戦力に、採用 / つなぎ / 育成中 / 育成予定 / 候補を表示対象として追加
- 図鑑一覧から厳選状態を直接切り替えられるボタンを追加
- 図鑑検索に、SpeciesRoleCandidateを使った役割フィルターを追加
- Excel更新後にmaster.jsonへ反映する手順を追記

## Ver1.0.2の改善内容

- 個体詳細・図鑑詳細の編集エリアを折りたたみ化
- 性格の上昇補正・下降補正を表示
- 固定表示の「一覧へ戻る」ボタンを追加
- 保存していない変更がある場合、戻る前に確認ダイアログを表示
- ポケモン選択をオートコンプリート方式へ改善
- 一覧へ戻る時にスクロール位置を復元
- ホームToDoから「島で確認」と「図鑑を見る」を選べるように改善
- 島画面、ToDo一覧、役割画面から関連する詳細画面へ移動できる導線を追加
- 島画面の現在戦力を睡眠タイプではなくタイプ基準で表示
- 島ごとの全出現ポケモン一覧を折りたたみで追加
- パンくずリストを追加

## 同梱内容

- `index.html`: アプリ本体の入口
- `sw.js`: Service Worker
- `README.md`: このファイル
- `public/master.json`: Excelから生成した正式マスターデータ
- `public/icon3.png`: PWAアイコン
- `public/manifest.json`: PWA manifest
- `src/`: 画面、Service、Store、Mapper
- `scripts/serve.ps1`: ローカル確認用サーバー
- `scripts/build-master-json.ps1`: Excelからmaster.jsonを生成するスクリプト
- `docs/master-json-generation.md`: master.json生成メモ

## Excel更新をアプリへ反映する手順

Excel「ポケスリメインデータ.xlsx」は正式なマスターデータです。アプリはExcelを直接読まず、Excelから生成した `public/master.json` を読み込みます。

1. Excel「ポケスリメインデータ.xlsx」を修正して保存します。
2. PowerShellでこのアプリのフォルダを開きます。
3. 以下を実行します。

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\build-master-json.ps1 -InputPath "C:\Users\kinoo\OneDrive\デスクトップ\ポケスリメインデータ.xlsx" -OutputPath public\master.json -AppVersion 1.0.3 -MasterVersion 2026.07.08 -MasterUpdatedAt 2026-07-08T00:00:00+09:00
```

4. `public/master.json` が更新されたことを確認します。
5. GitHubへ `public/master.json` をアップロードします。Excelファイル本体をGitHub Pages用にアップロードする必要はありません。
6. GitHub PagesのURLを開き、変更内容が反映されているか確認します。
7. スマホで確認する場合は、ホーム画面に追加したアプリを一度終了してから開き直します。

Service Workerのキャッシュが古い場合は、次の順に試してください。

1. ブラウザでページを再読み込みします。
2. ホーム画面に追加したPWAを完全に終了して開き直します。
3. まだ古い場合は、SafariのWebサイトデータ、またはブラウザのサイトデータを削除してから開き直します。
4. GitHub Pagesへアップロードした `public/master.json` が最新かを確認します。

## 起動方法

PowerShellでこのフォルダを開き、以下を実行します。

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\serve.ps1 -Port 4173
```

ブラウザで以下を開きます。

```text
http://127.0.0.1:4173/
```

PWAとService Workerの確認は、ローカルファイルを直接開くのではなく、必ずローカルサーバーまたはGitHub Pages経由で確認してください。

## GitHub Pagesで公開する手順

1. GitHubで新しいリポジトリを作成します。
2. ZIP内のファイルをリポジトリ直下にアップロードします。
3. GitHubのリポジトリ画面で `Settings` を開きます。
4. `Pages` を開きます。
5. `Build and deployment` の `Source` を `Deploy from a branch` にします。
6. `Branch` を `main`、フォルダを `/root` にして保存します。
7. 数分後に表示されるGitHub PagesのURLを開きます。

## GitHubへアップロードするファイル

以下をリポジトリ直下に置いてください。

- `index.html`
- `sw.js`
- `README.md`
- `public/`
- `src/`
- `scripts/`
- `docs/`

`work/` や `outputs/` は公開用には不要です。

## iPhoneでホーム画面へ追加する方法

1. iPhoneのSafariでGitHub PagesのURLを開きます。
2. 画面下部の共有ボタンを押します。
3. 「ホーム画面に追加」を選びます。
4. 名前を確認して「追加」を押します。
5. ホーム画面に `icon3.png` のアイコンが表示されることを確認します。
6. ホーム画面のアイコンから起動し、通常のブラウザ表示ではなくPWAとして開くことを確認します。

## JSONバックアップ

メニュー画面の「バックアップと復元」から `JSONバックアップ` を押すと、ユーザーデータだけを書き出せます。

バックアップJSONには以下が含まれます。

- `appVersion`
- `masterVersion`
- `exportedAt`
- `userData`

`public/master.json` 本体はバックアップに含まれません。

## JSON復元

メニュー画面の「バックアップと復元」から `JSONから復元` を押し、バックアップJSONを選択します。

復元前には確認ダイアログが表示されます。復元すると、現在のユーザーデータはバックアップ内容で置き換わります。

## 初回確認ポイント

- `Master 2026.07.06` が表示される
- 下部タブが `ホーム / 個体 / 図鑑 / 島 / メニュー` の5つで表示される
- 図鑑にポケモンが表示される
- 図鑑詳細で厳選設定と種族役割候補を編集できる
- 個体登録・個体編集ができる
- サブスキルをLv10、Lv25、Lv50、Lv70、Lv80で登録できる
- 役割画面できのみ・食材・スキルの目標を編集できる
- ホームToDoから島画面と図鑑詳細へ移動できる
- 島画面で現在戦力と全出現ポケモンを確認できる
- メニューから未設定一覧を開ける
- JSONバックアップを書き出せる

## 既知の制限

- ワカクサ本島 / ワカクサ本島EXの現在戦力用タイプ選択は画面状態のみで、IndexedDBには永続保存しません。
- 役割画面から個体登録へ移動した場合、対象役割の自動反映までは行いません。登録後に個体詳細で役割を保存してください。
- サブスキルは保存・表示のみで、スコア計算や自動評価には未接続です。
- 食材アイコンは正式画像素材ではなく、暫定の絵文字/ラベル表示です。
- master.jsonを更新した場合、Service Workerのキャッシュが残ることがあります。ブラウザ更新、PWA再起動、またはサイトデータ削除で確認してください。

## 今後の改善予定 Ver1.1候補

- 検索機能の強化
- フィルター追加
- 並び替え
- お気に入り強化
- 入力補助
- ワカクサ本島 / ワカクサ本島EXのタイプ選択永続保存
- 役割画面から個体登録した時の役割自動反映
- OCRによるスクリーンショット登録
